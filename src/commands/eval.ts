
import { MessageContext } from 'vk-io';
import { Command } from './index';

export default {
    name: "eval",
    desc: "локальный eval",
    aliases: ["eval", "жс"],
    handler(ctx, rctx) {

    }
} as Command<MessageContext>